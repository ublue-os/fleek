linters:
  presets:
    - bugs
    - unused
  enable:
    - gofmt
    - misspell
    - revive
    - stylecheck
  disable:
    # fleek is not a web server, context is not strictly necessary.
    - noctx